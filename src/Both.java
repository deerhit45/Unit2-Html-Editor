//decorator that has bot a parameter and an attribute
public class Both implements TagComponents
{
    //construcor
    public void both()
    {
    }
    
    //gets the text of the html tag to build a string or something
    public String getText()
    {
        //call super.getText to get wrapped tag text
        //insert both into the name string and return it
        return "";
    }
}